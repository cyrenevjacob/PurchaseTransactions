version: 0.1

phases:
 build:
  commands:
   - echo Entering build phase
   - mvn clean package
 post_build:
  commands:
   - echo Build completed successfully.
artefacts:
 files:
  - target/PurchaseTransaction.jar
 discard-paths: yes